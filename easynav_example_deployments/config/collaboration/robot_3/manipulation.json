{
  "name": "manipulation",
  "display_name": "Execute a manipulator trajectory",
  "description": "Executes a pre-planned joint-space (or Cartesian) trajectory on the robot's manipulator arm. This capability does not plan motions itself: the caller (typically a MoveIt planning step running elsewhere) must supply a complete, collision-free trajectory to execute. Use this once a target end-effector pose or joint configuration has already been resolved into concrete waypoints.",
  "action": {
    "name": "/robot_3/manipulation",
    "type": "moveit_msgs/action/ExecuteTrajectory",
    "goal": {
      "trajectory": "moveit_msgs/RobotTrajectory - the pre-planned trajectory to execute, as a trajectory_msgs/JointTrajectory (joint_trajectory) and/or a trajectory_msgs/MultiDOFJointTrajectory (multi_dof_joint_trajectory) for any virtual/floating joints.",
      "controller_names": "string[] - optional list of controller names that should execute the trajectory; leave empty to let the implementation pick the default controller(s) for the involved joints."
    },
    "feedback": {
      "state": "string - human-readable execution state of the trajectory (e.g. 'EXECUTING')."
    },
    "result": {
      "error_code": "moveit_msgs/MoveItErrorCodes - val == 1 (SUCCESS) on success. Non-1 values follow the standard MoveIt error code enum, notably -7 (PREEMPTED) if canceled or preempted, 99999 (FAILURE) on shutdown."
    }
  },
  "requirements": [
    "The trajectory must have already been planned (e.g. by a MoveIt planning pipeline) and be free of collisions; this capability performs no planning or collision checking of its own.",
    "The trajectory's joint names must match the robot's actual manipulator joints.",
    "The start state of the trajectory must be reasonably close to the robot's current state."
  ],
  "effects": [
    "On success (error_code.val == SUCCESS), the manipulator's joints end up at the final waypoint of the trajectory.",
    "A currently executing trajectory can be preempted by a newer goal (see 'preemption' below): the previous goal is aborted and the manipulator starts executing the new trajectory instead."
  ],
  "preemption": {
    "supported": true,
    "controlled_by_parameter": "manipulation.allow_preemption",
    "behavior_when_enabled": "Sending a new goal while one is active aborts the current trajectory execution and immediately starts executing the new one.",
    "behavior_when_disabled": "Sending a new goal while one is active is rejected outright; the caller must wait for the current execution to finish, or explicitly cancel it, before sending another."
  },
  "parameters": {
    "manipulation.allow_preemption": {
      "type": "bool",
      "default": true,
      "description": "Whether a new trajectory goal is allowed to preempt (replace) one that is currently executing."
    },
    "manipulation.mock_execution_duration": {
      "type": "double",
      "unit": "seconds",
      "default": 3.0,
      "description": "Mock/simulated time it takes to execute a trajectory, regardless of its actual number of waypoints. This implementation does not drive a real manipulator: it simulates progress over this duration for integration testing of the wider system."
    },
    "manipulation.mock_feedback_period": {
      "type": "double",
      "unit": "seconds",
      "default": 0.3,
      "description": "How often feedback is published while a goal is executing."
    },
    "capabilities_file": {
      "type": "string",
      "description": "Path to this very capability description file; published verbatim on /capabilities when the node is activated."
    }
  },
  "notes": [
    "This is a mock implementation, part of the arch_mockup architecture, standing in for a real MoveIt-driven manipulator during integration testing of multi-capability systems. It does not perform real trajectory execution or controller interaction: 'state' feedback simply reports 'EXECUTING' while progress is simulated on a timer.",
    "Deployment: 'collaboration' scenario, robot_3."
  ]
}
