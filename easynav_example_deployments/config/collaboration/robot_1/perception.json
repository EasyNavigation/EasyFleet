{
  "name": "perception",
  "display_name": "Detect objects of a known class",
  "description": "Runs a continuous object-detection stream for a requested object class, publishing detections (both 2D image-plane and 3D spatial) as feedback until the caller cancels the goal. This mock implementation only knows how to detect one class: cats ('gato'). Any goal that does not request that class is rejected outright.",
  "action": {
    "name": "/robot_1/perception",
    "type": "perception_interfaces/action/Perception",
    "goal": {
      "objects": "string[] - object classes to look for. Must include 'gato' (case-insensitive substring match); any other request is rejected since this mock does not detect other classes."
    },
    "feedback": {
      "detections_3d": "vision_msgs/Detection3DArray - 1 to 3 randomly placed 3D detections per message, published continuously while the goal is active.",
      "detections_2d": "vision_msgs/Detection2DArray - the same detections' 2D image-plane bounding boxes, published alongside detections_3d."
    },
    "result": {
      "success": "bool - true if the goal ended because the caller canceled it (the expected way to stop this capability); false if it was preempted or aborted.",
      "error_msg": "string - populated when success is false, explaining why."
    }
  },
  "requirements": [
    "The requested 'objects' list must include the target class this mock supports (default 'gato'); otherwise the goal is rejected immediately.",
    "This action never completes on its own: it streams feedback indefinitely until the caller cancels the goal (or it is preempted by a new one)."
  ],
  "effects": [
    "None on the physical world: this is a perception-only capability that reports detections, it does not act on them.",
    "While a goal is active, detections_3d/detections_2d feedback is published at a steady rate (see 'perception.detection_rate_hz')."
  ],
  "preemption": {
    "supported": true,
    "controlled_by_parameter": "perception.allow_preemption",
    "behavior_when_enabled": "Sending a new goal while one is active aborts the previous goal (success=false) and starts detecting for the new request instead.",
    "behavior_when_disabled": "Sending a new goal while one is active is rejected outright; the caller must cancel the current goal first."
  },
  "parameters": {
    "perception.allow_preemption": {
      "type": "bool",
      "default": true,
      "description": "Whether a new perception goal is allowed to preempt (replace) one that is currently executing."
    },
    "perception.target_class": {
      "type": "string",
      "default": "gato",
      "description": "The only object class this mock can 'detect'. A goal is only accepted if its 'objects' list contains this string (case-insensitive substring match)."
    },
    "perception.detection_rate_hz": {
      "type": "double",
      "unit": "hertz",
      "default": 20.0,
      "description": "Rate at which detections_3d/detections_2d feedback is published while a goal is active."
    },
    "perception.frame_id": {
      "type": "string",
      "default": "camera_link",
      "description": "Frame id stamped on the published detections."
    },
    "capabilities_file": {
      "type": "string",
      "description": "Path to this very capability description file; published verbatim on /capabilities when the node is activated."
    }
  },
  "notes": [
    "This is a mock implementation, part of the arch_mockup architecture, standing in for a real object detector during integration testing of multi-capability systems. Detections are random placeholder positions, not derived from any sensor data.",
    "Deployment: 'collaboration' scenario, robot_1."
  ]
}
